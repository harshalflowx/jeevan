# This is an example .env file.
# Create a copy named .env and fill in your actual values.
# Do NOT commit your .env file with real secrets to version control.

# For AuthManager - The SHA256 hash of your chosen admin API key.
# To generate: python -c "import hashlib; print(hashlib.sha256('YOUR_CHOSEN_PLAIN_API_KEY'.encode()).hexdigest())"
AI_ADMIN_HASHED_KEY="your_sha256_hashed_admin_api_key_here"

# For GeminiServiceConnector
GEMINI_API_KEY="your_gemini_api_key_here"
# Optional: If you want to override the default Gemini API endpoint in gemini_service_connector.py
# GEMINI_API_ENDPOINT="your_specific_gemini_api_endpoint_here"

# The default API key used by CommandInterface if a command doesn't specify one.
# This should be the PLAIN TEXT key corresponding to AI_ADMIN_HASHED_KEY for most operations.
# This is more for reference as CommandInterface has it hardcoded, but useful if centralized.
# DEFAULT_USER_API_KEY="test_api_key_123"
